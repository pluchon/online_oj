# 复制为 .env 后填写；.env 不入库
# MySQL root 密码（不填时 compose 使用本地默认值）
MYSQL_ROOT_PASSWORD=
# Nacos 3.x 令牌密钥：至少 32 个字符的随机串再做 Base64 编码
NACOS_AUTH_TOKEN=
# Nacos 3.x 服务端之间的身份标识
NACOS_AUTH_IDENTITY_KEY=
NACOS_AUTH_IDENTITY_VALUE=
